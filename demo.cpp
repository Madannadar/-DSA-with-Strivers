#include <iostream>
using namespace std;  // is used so that we do not need to write sdt:: always

int main() {
    int userInput;
    cin >> userInput;
    cout << "You entered: " << userInput << "," ;
    cout << " its over";
    return 0;
}
